DROP TABLE IF EXISTS order_analytics;
	CREATE TABLE order_analytics
	SELECT id, YEAR(order_date) AS year, QUARTER(order_date) AS quarter,  
	type, quantity*price AS total_price FROM orders;
	

	SELECT *
	FROM order_analytics
	ORDER by id;